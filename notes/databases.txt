// Users database
CREATE DATABASE newapp;
USE newapp;
CREATE TABLE users (
  id INT NOT NULL UNIQUE AUTO_INCREMENT PRIMARY KEY,
  uname VARCHAR(26) NOT NULL UNIQUE,
  email VARCHAR(256) NOT NULL UNIQUE,
  hash VARCHAR(256) NOT NULL,
  admin INT DEFAULT 0,
  session VARCHAR(256) DEFAULT -1
);
